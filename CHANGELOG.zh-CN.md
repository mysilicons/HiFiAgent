# 变更日志

[English](CHANGELOG.md) | **简体中文**

所有重要变更均记录于此。本项目遵循语义化版本。

## [3.0.0] - 2026-08-13

### 新增

- 可移植 fixture 工具链，无需生物数据即可经过真实 CLI、子进程边界、工作流运行器、解析器、
  参数合同、报告和深度验证器。
- 经过校验和保护、与轮次绑定的录制模型 transcript replay，用于离线 hybrid 验收。
- 面向科学停止、人工复核、工具失败、必需服务失败和中断恢复的稳定 CLI 退出与报告合同。
- package 自带生产工作流、比较策略和治理知识资源，使 wheel 可脱离源码 checkout 运行。
- 严格分离的全局运行配置与单样本配置，以及单命令执行方式。
- 带锁的 BUSCO lineage 缓存、不可变源快照、自动恢复和验证后保留策略。
- 外部真实数据注册表、严格运行验证、在线服务烟雾测试和哈希绑定证据包接口，真实数据不进入 Git。

### 变更

- 生产生命周期统一由一个权威协调器和一个 run state 管理，轮次、终态和类型化产物服务相互分离。
- 治理检索保持来源多样性，并在授权参数前验证工具版本证据。
- Baseline 与 candidate attempt 共用相同执行器和 post-QC 协议。
- Nextflow SIGINT 和 SIGTERM 被分类为可恢复中断。
- 发行包通过隔离 wheel 的可移植执行进行检查。

### 移除

- 已弃用的兼容命令、迁移读取器、执行适配器和别名。
- 仓库绑定的真实输入路径和未治理开发产物。

## [2.0.0] - 2026-07-29

### 新增

- 不可变 run、attempt 和 round Schema，以及单一编排控制器。
- 类型化 QC 特征、证据治理检索、严格结构化提案解析和确定性 Safety Arbiter。
- Candidate argv 往返合同、唯一 attempt 目录和执行回执。
- 具有硬回退、平台期、冲突、预算和有界轮次终态的多指标比较策略。
- 可追踪批准参数、实际 argv 和产物的 JSON、Markdown 与 TSV 报告。
- 隐私和成本披露、可重复三轮演示和干净构建验证。

### 变更

- 结构化服务可以提出白名单候选，但只有确定性 Safety Arbiter 能批准完整可执行配置。
- 默认优化为每轮一个候选，最多三轮。
- 比较策略打包进 wheel，不再从源代码树读取。

### 已知限制

- 使用同一批 reads 的 Merqury 证据仅供参考，优先使用独立 reads。
- 外部服务价格不冻结；回执保留 token、模型、endpoint 类别和响应哈希，但不保留凭据。

## [1.0.0] - 2026-07-14

### 新增

- 经过验证的单样本 PacBio HiFi 配置与 provenance 回执。
- Nextflow DSL2 pre-QC、hifiasm baseline、post-QC 与恢复工作流。
- 有界专家规则、明确停止状态和受约束解释层。
- 使用多指标硬门禁的一轮候选比较。
- 带敏感路径脱敏的 Markdown、JSON 与 TSV 报告。
- 可移植演示、用户和开发指南、规则目录与引用元数据。

### 已知限制

- 仅支持 HiFi 单样本组装；不支持 Hi-C、trio、ONT、scaffolding、注释和无界参数搜索。
- 使用同一批 reads 的 k-mer 评估属于建议性证据，而非独立验证。
- 检索与模型解释永远不能授权参数或执行 shell。
